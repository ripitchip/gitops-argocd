apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth2-proxy-policy-reporter
  namespace: policy-reporter
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: oauth2-proxy-policy-reporter
  template:
    metadata:
      labels:
        k8s-app: oauth2-proxy-policy-reporter
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 5678
        runAsGroup: 5678
        fsGroup: 5678
      containers:
        - name: oauth2-proxy
          image: quay.io/oauth2-proxy/oauth2-proxy:v7.6.0
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 5678
            runAsGroup: 5678
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          resources:
            limits:
              cpu: 200m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 128Mi
          args:
            - --provider=oidc
            - --oidc-issuer-url=https://auth.foobarbaz.fr/realms/master
            - --email-domain=*
            - --http-address=0.0.0.0:4180
            - --upstream=http://policy-reporter-ui.policy-reporter.svc.cluster.local:8080
            - --cookie-secure=true
            - --cookie-samesite=lax
            - --set-authorization-header=true
            - --pass-access-token=true
            - --redirect-url=https://policy-reporter.foobarbaz.fr/oauth2/callback
            - --insecure-oidc-skip-issuer-verification=true
            - --reverse-proxy=true
            - --allowed-group=policy-reporter-admin
            - --scope=openid profile email groups
          env:
            - name: OAUTH2_PROXY_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: oauth2-proxy-policy-reporter
                  key: client-id
            - name: OAUTH2_PROXY_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: oauth2-proxy-policy-reporter
                  key: client-secret
            - name: OAUTH2_PROXY_COOKIE_SECRET
              valueFrom:
                secretKeyRef:
                  name: oauth2-proxy-policy-reporter
                  key: cookie-secret
          ports:
            - containerPort: 4180
---
apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy-policy-reporter
  namespace: policy-reporter
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 4180
  selector:
    k8s-app: oauth2-proxy-policy-reporter
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: policy-reporter-ingress
  namespace: policy-reporter
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: traefik
  rules:
  - host: policy-reporter.foobarbaz.fr
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: oauth2-proxy-policy-reporter
            port:
              number: 80
  tls:
  - hosts:
    - policy-reporter.foobarbaz.fr
    secretName: policy-reporter-tls
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: oauth2-proxy-policy-reporter
  namespace: policy-reporter
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: oauth2-proxy-policy-reporter
    creationPolicy: Owner
  data:
  - secretKey: client-id
    remoteRef:
      key: oauth2-proxy
      property: client-id
  - secretKey: client-secret
    remoteRef:
      key: oauth2-proxy
      property: client-secret
  - secretKey: cookie-secret
    remoteRef:
      key: oauth2-proxy
      property: cookie-secret
---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: oauth2-proxy-policy-reporter-policy
  namespace: policy-reporter
spec:
  endpointSelector:
    matchLabels:
      k8s-app: oauth2-proxy-policy-reporter
  ingress:
    # Allow Traefik to access OAuth2-Proxy
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: traefik
      toPorts:
        - ports:
            - port: "4180"
              protocol: TCP
  egress:
    # 1. Access to the UI
    - toEndpoints:
        - matchLabels:
            app.kubernetes.io/name: policy-reporter-ui
      toPorts:
        - ports:
            - port: "8080"
              protocol: TCP
    # 2. Access to Keycloak (Identity Provider)
    - toEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: auth
            app.kubernetes.io/name: keycloak
      toPorts:
        - ports:
            - port: "8080"
              protocol: TCP
    # 3. DNS Resolution
    - toEndpoints:
        - matchLabels:
            "k8s:io.kubernetes.pod.namespace": kube-system
            "k8s:k8s-app": kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: ANY
