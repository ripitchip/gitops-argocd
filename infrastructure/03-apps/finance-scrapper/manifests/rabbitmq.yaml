apiVersion: apps/v1
kind: Deployment
metadata:
  name: finance-scrapper-rabbitmq
  namespace: finance-scrapper
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: finance-scrapper-rabbitmq
  template:
    metadata:
      labels:
        app: finance-scrapper-rabbitmq
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 29097
        runAsGroup: 29097
        fsGroup: 29097
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: rabbitmq
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 29097
            runAsGroup: 29097
          image: rabbitmq:3-management-alpine
          imagePullPolicy: IfNotPresent
          env:
            - name: RABBITMQ_DEFAULT_USER
              value: advisor
            - name: RABBITMQ_DEFAULT_PASS
              valueFrom:
                secretKeyRef:
                  name: finance-scrapper-secrets
                  key: RABBITMQ_DEFAULT_PASS
          ports:
            - name: amqp
              containerPort: 5672
            - name: management
              containerPort: 15672
          resources:
            limits:
              cpu: 500m
              memory: 1Gi
            requests:
              cpu: 100m
              memory: 256Mi
          volumeMounts:
            - name: rabbitmq-data
              mountPath: /var/lib/rabbitmq
            - name: rabbitmq-config
              mountPath: /etc/rabbitmq/rabbitmq.conf
              subPath: rabbitmq.conf
      volumes:
        - name: rabbitmq-data
          emptyDir: {}
        - name: rabbitmq-config
          configMap:
            name: finance-scrapper-rabbitmq-config
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: finance-scrapper-rabbitmq-config
  namespace: finance-scrapper
data:
  rabbitmq.conf: |
    # Increase maximum message size limit to 512MB (536870912 bytes)
    # This prevents ChannelPreconditionFailed errors on large message collection payloads.
    max_message_size = 536870912
---
apiVersion: v1
kind: Service
metadata:
  name: finance-scrapper-rabbitmq
  namespace: finance-scrapper
spec:
  selector:
    app: finance-scrapper-rabbitmq
  ports:
    - name: amqp
      port: 5672
      targetPort: 5672
    - name: management
      port: 15672
      targetPort: 15672
