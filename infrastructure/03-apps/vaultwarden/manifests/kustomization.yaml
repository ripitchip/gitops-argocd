apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - storage.yaml
  - external-secrets.yaml
  - policy.yaml
  - app.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: vaultwarden-stack
    includeSelectors: true