rules:
  - id: generic-api-key
    category: secret
    title: Generic API Key
    severity: HIGH
    regex: '(?i)(api[_-]?key|token|secret|password)["'']?[[:space:]]*[:=][[:space:]]*["'']?[A-Za-z0-9_./+=-]{20,}'
