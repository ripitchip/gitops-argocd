apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - config.yaml
  - app.yaml
  - ingress.yaml
  - baseline-network-policy.yaml
  - policy.yaml
  - edge-middlewares.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: startpage
    includeSelectors: true
