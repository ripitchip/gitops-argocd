apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: ntfy-oauth2-proxy-auth
  namespace: notifications
spec:
  forwardAuth:
    # Utilisation de l'endpoint d'authentification et du port standard 80
    address: http://ntfy-oauth2-proxy.notifications.svc.cluster.local/oauth2/auth
    trustForwardHeader: true
    authResponseHeaders:
      - X-Auth-Request-User
      - X-Auth-Request-Email
      - X-Auth-Request-Access-Token
      - Authorization
