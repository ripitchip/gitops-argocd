apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: radicale-ingress
  namespace: radicale
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    ingress.cilium.io/loadbalancer-mode: "shared"
spec:
  ingressClassName: cilium
  tls:
    - hosts:
        - cal.tomlab.fr
        - cal.foobarbaz.fr
      secretName: radicale-tls
  rules:
    - host: cal.tomlab.fr
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: radicale
                port:
                  number: 80
    - host: cal.foobarbaz.fr
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: radicale
                port:
                  number: 80

