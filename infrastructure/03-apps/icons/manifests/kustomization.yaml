apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - storage.yaml
  - sync-job.yaml
  - app.yaml
  - ingress.yaml
  - baseline-network-policy.yaml
  - policy.yaml
  - edge-middlewares.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: icons
    includeSelectors: true
