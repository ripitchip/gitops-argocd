apiVersion: v1
kind: ConfigMap
metadata:
  name: radicale-config
  namespace: radicale
data:
  config: |
    [server]
    hosts = 0.0.0.0:5232

    [auth]
    type = radicale_modoboa_auth_oauth2
    oauth2_introspection_endpoint = https://auth.foobarbaz.fr/realms/foobarbaz/protocol/openid-connect/token/introspect

    [storage]
    filesystem_folder = /data/collections
