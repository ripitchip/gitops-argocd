apiVersion: apps/v1
kind: Deployment
metadata:
  name: opencloud
  namespace: opencloud
  labels:
    app: opencloud
spec:
  replicas: 1
  selector:
    matchLabels:
      app: opencloud
  template:
    metadata:
      labels:
        app: opencloud
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 62403
        runAsGroup: 62403
        fsGroup: 62403
      containers:
        - name: opencloud
          image: opencloudeu/opencloud:7.2.4
          command: ["sh", "-c", "if [ ! -f /etc/opencloud/opencloud.yaml ]; then rm -rf /var/lib/opencloud/*; opencloud init; fi && opencloud server"]
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 62403
            seccompProfile:
              type: RuntimeDefault
          env:
            - name: OC_URL
              value: "https://drive.tomlab.fr"
            - name: OC_INSECURE
              value: "true"
            - name: PROXY_HTTP_ADDR
              value: "0.0.0.0:9200"
            - name: PROXY_TLS
              value: "false"
            - name: OC_LOG_LEVEL
              value: "info"
            # Keycloak External IDP Integration & CSP
            - name: OC_OIDC_ISSUER
              value: "https://auth.foobarbaz.fr/realms/foobarbaz"
            - name: OC_EXCLUDE_RUN_SERVICES
              value: "idp"
            - name: PROXY_OIDC_ACCESS_TOKEN_VERIFY_METHOD
              value: "jwt"
            - name: PROXY_OIDC_REWRITE_WELLKNOWN
              value: "true"
            - name: PROXY_USER_OIDC_CLAIM
              value: "preferred_username"
            - name: PROXY_USER_CS3_CLAIM
              value: "username"
            - name: WEB_OPTION_ACCOUNT_EDIT_LINK_HREF
              value: "https://auth.foobarbaz.fr/realms/foobarbaz/account"
            - name: PROXY_AUTOPROVISION_ACCOUNTS
              value: "true"
            - name: GRAPH_ASSIGN_DEFAULT_USER_ROLE
              value: "true"
            - name: OC_LDAP_SERVER_WRITE_ENABLED
              value: "true"
            - name: GRAPH_USERNAME_MATCH
              value: "none"
            - name: PROXY_ROLE_ASSIGNMENT_DRIVER
              value: "default"
            - name: WEBFINGER_WEB_OIDC_CLIENT_ID
              value: "web"
            - name: WEBFINGER_WEB_OIDC_CLIENT_SCOPES
              value: "openid profile email"
            - name: PROXY_CSP
              value: "default-src 'self' 'unsafe-inline' 'unsafe-eval'; img-src 'self' data: https:; connect-src 'self' https://auth.foobarbaz.fr https://drive.tomlab.fr; frame-src 'self' https://auth.foobarbaz.fr; frame-ancestors 'self';"
            - name: HTTP_CSP
              value: "default-src 'self' 'unsafe-inline' 'unsafe-eval'; img-src 'self' data: https:; connect-src 'self' https://auth.foobarbaz.fr https://drive.tomlab.fr; frame-src 'self' https://auth.foobarbaz.fr; frame-ancestors 'self';"
            # JWT & Internal Auth Secrets
            - name: OC_JWT_SECRET
              value: "a-super-secret-jwt-key-for-opencloud-authentication-32bytes"
            - name: JWT_SECRET
              value: "a-super-secret-jwt-key-for-opencloud-authentication-32bytes"
            - name: OC_SERVICE_ACCOUNT_SECRET
              value: "opencloud-service-account-secret-32bytes"
            - name: SERVICE_ACCOUNT_SECRET
              value: "opencloud-service-account-secret-32bytes"
            - name: OC_TRANSFER_SECRET
              value: "opencloud-transfer-secret-32bytes"
            - name: OC_MACHINE_AUTH_API_KEY
              value: "opencloud-machine-auth-api-key-32bytes"
            # Storage & Metadata Backend
            - name: OC_STORAGE_USERS_DRIVER
              value: "ocis"
            - name: STORAGE_USERS_DRIVER
              value: "ocis"
            - name: OC_STORAGE_USERS_OCIS_USER_METADATA_BACKEND
              value: "sqlite"
            - name: STORAGE_USERS_OCIS_USER_METADATA_BACKEND
              value: "sqlite"
            - name: OC_STORAGE_SYSTEM_DRIVER
              value: "ocis"
            - name: STORAGE_SYSTEM_DRIVER
              value: "ocis"
            - name: OC_STORAGE_SYSTEM_OCIS_USER_METADATA_BACKEND
              value: "sqlite"
            - name: STORAGE_SYSTEM_OCIS_USER_METADATA_BACKEND
              value: "sqlite"
            - name: OC_STORAGE_METADATA_DRIVER
              value: "sqlite"
            - name: STORAGE_METADATA_DRIVER
              value: "sqlite"
          ports:
            - containerPort: 9200
              name: http
          readinessProbe:
            tcpSocket:
              port: 9200
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 9200
            initialDelaySeconds: 10
            periodSeconds: 20
          volumeMounts:
            - name: data
              mountPath: /var/lib/opencloud
            - name: config
              mountPath: /etc/opencloud
              subPath: config
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: opencloud-data-pvc
        - name: config
          persistentVolumeClaim:
            claimName: opencloud-data-pvc
