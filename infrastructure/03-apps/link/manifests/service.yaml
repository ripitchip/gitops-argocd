apiVersion: v1
kind: Service
metadata:
  name: karakeep
  namespace: karakeep
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
spec:
  selector:
    app: karakeep
  ports:
    - port: 80
      targetPort: 3000
